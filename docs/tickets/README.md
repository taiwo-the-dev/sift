# Ticket history

Completed milestone tickets record the requirements that applied when each
milestone was implemented. They are retained for traceability.

The current release boundary is defined by
[`MASTER-001-sift.md`](MASTER-001-sift.md): Sift supports isolated BSC Mainnet
chain `56` and BSC Testnet chain `97` experiences. Do not add another network
without an explicitly approved milestone.
