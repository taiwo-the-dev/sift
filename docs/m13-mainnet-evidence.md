# M13 BSC mainnet evidence

Observed and verified on 2026-08-25. This record contains public configuration
and aggregate database evidence only; it contains no RPC or Supabase secret.

## Authoritative configuration

| Evidence | Verified value | Source |
| --- | --- | --- |
| BSC mainnet chain | `56` | [Official BNB Agent SDK networks](https://docs.bnbchain.org/developer-kit/bnbagent-sdk/networks/) |
| BSC mainnet Identity Registry | `0x8004A169FB4a3325136EB29fA0ceB6D2e539a432` | [Official BNB Agent SDK](https://github.com/bnb-chain/bnbagent-sdk) and [canonical ERC-8004 contracts](https://github.com/erc-8004/erc-8004-contracts) |
| Deployment block | `79,027,268` | Historical BSC bytecode boundary and block receipt |
| Deployment block hash | `0xdb9c6a8fff62cc59b2e2d9978af06db139a41e65f30d99ea6f12dc58909d5a36` | BSC JSON-RPC `eth_getBlockByNumber` |
| Explorer origin | `https://bscscan.com` | [Official BNB wallet configuration](https://docs.bnbchain.org/bnb-smart-chain/developers/wallet-configuration/) |
| RPC limitation | Official mainnet endpoints may disable `eth_getLogs`; third-party endpoints are recommended | [Official BNB JSON-RPC endpoint guide](https://docs.bnbchain.org/bnb-smart-chain/developers/json_rpc/json-rpc-endpoint/) |

Historical `eth_getCode` returned no code at block `79,027,267` and registry
code at `79,027,268`. The checked-in ABI's indexed events and read functions
match the official BNB Agent SDK ABI. A read-only M13 smoke on a recent
100-block range passed at confirmed block `118,014,033` and decoded six registry
events. That proves current read configuration, not historical bootstrap
capacity.

## Hosted snapshot before the M13 migration

Snapshot time: `2026-08-25T14:14:38Z`.

| Network | Agent count | Last checkpoint | Checkpoint observed |
| --- | ---: | ---: | --- |
| BSC mainnet (`56`) | `0` | None | None |
| BSC Testnet (`97`) | `1,890` | `126,540,754` | `2026-08-22T09:09:32.424706Z` |

The snapshot was generated by service-role count/checkpoint reads scoped to the
verified chain and registry pairs. It must not be presented as current after a
bootstrap; rerun `npm run report:catalogue` for the release record.

## Open eligibility and infrastructure evidence

- The hosted Supabase project was reported as `paused` by the authenticated CLI
  on 2026-08-25. The M13 migration therefore could not be linked, dry-run, or
  applied. The project owner must unpause it in Supabase, then allow the GitHub
  integration to deploy the ordered migration before the M13 UI can query the
  new network-scoped function.
- Recent-range public RPC reads pass, but the tested unauthenticated providers
  either reject historical state, cap `eth_getLogs` at very small ranges, or
  rate-limit the full deployment-to-head bootstrap. M13 therefore needs one
  archive-capable free-tier BSC RPC URL in `BNB_MAINNET_RPC_PRIMARY`.
- The organizer question “Does ‘live on BSC’ permit BSC Testnet ERC-8004
  identities, or must surfaced agents be registered on chain 56?” is prepared
  but has not been sent because no official support-channel identity or posting
  authority is available in this repository. Record a linked organizer answer
  here when the project owner receives it. Mainnet indexing remains the safer
  requirement regardless.
- No chain-97 row will be copied or relabelled. M13 passes only after a
  source-backed chain-56 bootstrap and independent caught-up checkpoint appear
  in the operator report.
